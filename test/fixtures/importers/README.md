# importers/

Real vendor CSV samples (ForeFlight `logbook_template.csv`, Garmin Pilot
logbook exports) for testing `io/` adapters. See `test/fixtures/README.md`
for the provenance and scrubbing requirements before committing a sample.
